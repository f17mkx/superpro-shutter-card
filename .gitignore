# Dependencies
node_modules/
.pnpm-store/

# Build output
# NOTE: dist/ IS the shipped artifact for this HACS card - committed so
# users without a build step still work. v0.5 added a Rollup pipeline that
# rebuilds dist/superpro-shutter-card.js from src/, but the output is the
# same bytes that get committed (CI guards against drift).
build/
*.tsbuildinfo

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env / secrets
.env
.env.local
.env.*.local

# Test artefacts
coverage/
.nyc_output/
playwright-report/
test-results/

# Conductor / workspace-local
.context/
session-logs/
